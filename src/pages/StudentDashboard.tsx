import { Link } from 'react-router-dom';
import { Award, BarChart3, Flame, Trophy } from 'lucide-react';
import { projects } from '../data/projects';

export default function StudentDashboard(){return <main className="min-h-screen bg-slate-950 px-6 py-10 text-white"><div className="mx-auto max-w-7xl"><h1 className="text-4xl font-black">Welcome back, Ama Mensah!</h1><p className="mt-2 text-slate-300">Continue learning by building practical maths projects.</p><div className="mt-8 grid gap-4 md:grid-cols-4">{[['Missions', '6 active', Trophy], ['Score','1,250 pts', BarChart3], ['Badges','3 earned', Award], ['Streak','5 days', Flame]].map(([label,value,Icon])=>{const I=Icon as typeof Trophy;return <div key={String(label)} className="card p-5"><I className="text-yellow-300"/><p className="mt-3 text-slate-300">{String(label)}</p><p className="text-2xl font-black">{String(value)}</p></div>})}</div><div className="mt-8 card p-6"><p className="text-sm text-emerald-300">Continue mission</p><h2 className="text-2xl font-black">Build a Footbridge Over a Stream</h2><Link className="btn-primary mt-4 inline-block" to="/student/project/footbridge-stream">Continue Mission</Link></div><h2 className="mt-10 text-3xl font-black">Active worlds</h2><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.map(project=><Link key={project.id} to={`/student/project/${project.id}`} className="card p-5 hover:bg-white/15"><p className="text-emerald-300">{project.world}</p><h3 className="mt-2 font-black">{project.title}</h3></Link>)}</div></div></main>}
