import { Link } from 'react-router-dom';
import { projects } from '../data/projects';

export default function WorldsPage(){return <main className="min-h-screen bg-slate-950 px-6 py-10 text-white"><div className="mx-auto max-w-7xl"><h1 className="text-4xl font-black">Choose Your Maths World</h1><p className="mt-2 text-slate-300">Only beginner missions are active in this MVP. Other modes are coming soon.</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{projects.map(project=><div key={project.id} className="card p-6"><p className="text-sm text-emerald-300">{project.world}</p><h2 className="mt-2 text-2xl font-black">{project.title}</h2><p className="mt-3 text-slate-300">{project.scenario}</p><div className="mt-4 flex flex-wrap gap-2">{project.concepts.slice(0,3).map(c=><span key={c} className="rounded-full bg-white/10 px-3 py-1 text-xs">{c}</span>)}</div><Link className="btn-primary mt-5 inline-block" to={`/student/project/${project.id}`}>Start Mission</Link></div>)}</div></div></main>}
